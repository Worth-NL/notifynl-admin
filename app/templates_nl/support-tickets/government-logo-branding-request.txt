Organisation: {% if current_service.organisation -%}
  {{ current_service.organisation.name }}
{%- else -%}
  Niet te bepalen (domein is {{ current_user.email_domain }})
{%- endif %}
Service: {{ current_service.name }}
{{ url_for('main.service_dashboard', service_id=current_service.id, _external=True) }}

---

## Maak een nieuw overheidsidentiteitslogo aan
{% if branding_choice == "govuk_and_org" %}
{% set create_logo_url = url_for(
'main.create_email_branding_government_identity_logo', text=logo_text, brand_type="both", _external=True
) %}
{% else %}
{% set create_logo_url = url_for(
'main.create_email_branding_government_identity_logo', text=logo_text, _external=True
) %}
{% endif %}

Open deze link om een nieuw overheidsidentiteitslogo aan te maken: {{ create_logo_url }}

1. Selecteer het wapen of het insigne voor {% if current_service.organisation %}{{current_service.organisation.name}}. Als deze organisatie niet in de lijst staat, selecteer dan 'Rijksoverheid'. {% else %}de organisatie waartoe de dienst behoort. Als de organisatie niet in de lijst staat, selecteer dan 'Rijksoverheid'.{% endif %}
2. Selecteer de streepkleur voor {% if current_service.organisation -%}{{current_service.organisation.name}}. Als deze organisatie niet in de lijst staat, selecteer dan 'Rijksoverheid'.{% else %}de organisatie waartoe de dienst behoort. Als de organisatie niet in de lijst staat, selecteer dan 'Rijksoverheid'.{% endif %}
3. Controleer of de logotekst luidt: {{ logo_text }}
4. Controleer of het geselecteerde brandtype is: {% if branding_choice == "govuk_and_org" %}NotifyNL en huisstijl{% else %}Alleen huisstijl{% endif %}
5. Selecteer 'Opslaan'.


## Stel de e-mailhuisstijl in voor deze service

Open deze link om de nieuwe e-mailhuisstijl te selecteren voor {{ current_service.name }}: {{ url_for('main.service_set_branding', service_id=current_service.id, branding_type='email', _external=True) }}


Deze service heeft organisatiebranding gevraagd.

{% endif %}Maak dit logo aan: {{ create_logo_url }}

Pas branding toe op deze service: {{ url_for('main.service_set_branding', service_id=current_service.id, branding_type='email', _external=True) }}
